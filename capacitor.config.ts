import { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.complexepetitpas.app',
  appName: 'Complexe Petit Pas',
  webDir: 'dist'
};

export default config;
